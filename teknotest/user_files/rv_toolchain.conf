# Define your RISC-V toolchain path here
# build.py exec's this file as Python and reads RISCV_GCC_PREFIX.
# Prefix = folder + tool stem (build.py appends -gcc/-objcopy/... and .exe on Windows).
#
# Linux example:
# RISCV_GCC_PREFIX = "/opt/riscv/bin/riscv32-unknown-elf"
# Windows example:
# RISCV_GCC_PREFIX = r"C:\RISC-V\bin\riscv32-unknown-elf"
#
# DDK note: judges re-point this to their own toolchain; only needs to work locally.

RISCV_GCC_PREFIX = r"C:\riscv\xpack-riscv-none-elf-gcc-15.2.0-1-win32-x64\xpack-riscv-none-elf-gcc-15.2.0-1\bin\riscv-none-elf"
